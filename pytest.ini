[pytest]
markers =
    unit: pure logic tests with no real database, HTTP client, or model artifact I/O
    integration: tests that exercise a real SQLite database, a real FastAPI TestClient, and/or a real persisted model artifact
filterwarnings =
    ignore::DeprecationWarning
    ignore::UserWarning